# Agent 06 — Release audit + packaging

## Objective
Run full smoke tests and package `QuoteGenV5.0.zip`.

## Tasks
1. Run the full test plan from plan zip `tests/TEST_PLAN.md`.
2. Verify Definition of Done from plan zip `tests/DEFINITION_OF_DONE.md`.
3. Run accessibility quick checks (shared/ACCESSIBILITY_CHECKS.md).
4. Verify `models/headers.py` is unchanged (byte-for-byte).
5. Create release zip named `QuoteGenV5.0.zip`.

## Evidence
- Copy/paste test checklist results
- Screenshot of final UI shell + sidebar + status + toasts

## Exit criteria
- All DoD items satisfied and zip created.
